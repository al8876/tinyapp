<!DOCTYPE html>
<html lang="en">
<head>
    <title>TinyApp</title>
    <% include ./partials/_header %>
</head>
<body>
  <h2>TinyApp</h2>
    <h3>Your URL List</h3>
      <table border="black"; cellpadding="10px">
        <tr align="left">
          <th><b>Long URL</b></th>
          <th><b>Short URL</b></th>
        </tr>
        <% if (user) { %>
          <tr>
            <% for (let key in urls) { %>
              <% if (urls[key].userID === user) { %>
              <td><%= urls[key].url %></td>
              <td><a href="/u/<%= key %>"><%= key %></a></td>
              <td><button><a href="/urls/<%= key %>/">Edit</a></button></td>
              <td><form action='/urls/<%= key %>/delete' method='post'><button type="submit">Delete 🗑️</button></form></td>
              <% } %>
          </tr>
          <% } %>
        <% } else { %>
          <tr>
            <td>Log in or register to see your links</td>
          </tr>
        <% } %>
      </table>
  <br>
<a href="/urls/new"><button>Link to Shorten URL</button></a>
</body>
</html>